{% extends 'admin/base.html.twig' %}

{% block title %}
	{% if user.id %}
	{% trans %}User Edit{% endtrans %} - {{ user.username }}
	{% else %}
	{% trans %}User Add{% endtrans %}
	{% endif %}
{% endblock %}

{% block body %}

<ol class="breadcrumb">
	<li class="breadcrumb-item">
		<a href="{{ path('admin_dashboard') }}">{% trans %}Dashboard{% endtrans %}</a>
	</li>
	<li class="breadcrumb-item">
		<a href="{{ path('admin_user') }}">{% trans %}Manager Users{% endtrans %}</a>
	</li>	
	<li class="breadcrumb-item active">
	{% if user.id %}
	{% trans %}User Edit{% endtrans %} - {{ user.username }}
	{% else %}
	{% trans %}User Add{% endtrans %}
	{% endif %}
	</li>
</ol>

<div class="mb-3">
	{{ include('blocks/message.html.twig') }}
	{{ form(form) }}
</div>
{% endblock %}
